@page
@model Costea_Maria_ClaudiaBakeryShop.Pages.Carts.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Cart</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Cart.ID" />
            <div class="form-group">
                <label asp-for="Cart.MemberID" class="control-label"></label>
                <select asp-for="Cart.MemberID" class="form-control" asp-items="ViewBag.MemberID"></select>
                <span asp-validation-for="Cart.MemberID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Cart.ProductID" class="control-label"></label>
                <select asp-for="Cart.ProductID" class="form-control" asp-items="ViewBag.ProductID"></select>
                <span asp-validation-for="Cart.ProductID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Cart.OrderDate" class="control-label"></label>
                <input asp-for="Cart.OrderDate" class="form-control" />
                <span asp-validation-for="Cart.OrderDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
